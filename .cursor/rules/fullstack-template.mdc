---
description: Fullstack-template monorepo project rule
globs:
  - "**/*"
alwaysApply: true
---

# fullstack-template 项目级规则

## 适用范围

- 本规则适用于仓库全部目录，优先约束 `apps/web`、`apps/server`、`packages/*`。
- 默认使用 Bun 工作区能力执行命令，不切换到其他包管理器工作流。

## 必须遵守

### 工作流与命令

- 使用根脚本作为统一入口：`bun run dev`、`bun run build`、`bun run lint`、`bun run test`。
- 代码生成使用 `bun run codegen:api`，数据库流程使用 `bun run db:generate`、`bun run db:migrate`、`bun run db:push`、`bun run db:studio`。
- 任何影响接口类型的后端变更后，必须同步执行 API codegen 并提交生成产物。

### 代码质量门禁

- 提交前必须通过 lint：`bun run lint`。
- 变更影响服务端逻辑时，至少执行 `cd apps/server && bun run test`。
- 变更影响前端页面、路由或接口交互时，至少执行 `cd apps/web && bun run test:e2e`。
- 尊重 git hooks：`pre-commit` 运行 `lint-staged`，`commit-msg` 运行 `commitlint`。
- 提交信息遵循 Conventional Commits。

### 目录与命名约定

- `apps/web`：目录 kebab-case；组件文件 PascalCase；普通 TS/TSX 文件 camelCase；资源文件 kebab-case。
- `apps/server`：目录与源文件 camelCase，并使用领域前缀（如 `userController.ts`、`userService.ts`）；测试文件使用 `*.test.ts`。
- `packages/shared` 仅放跨端复用类型与工具，避免耦合前后端运行时细节。
- `packages/elysia-plugin-response` 承载统一响应协议（runtime plugin + contract + schema builder），优先在 server 侧复用，不在业务代码重复实现。

## 前端约束（apps/web）

- `src/services/schema.gen.ts` 为自动生成文件，禁止手工修改。
- API 类型从生成的 `components['schemas']['...']` 引用。
- 请求统一使用 openapi-fetch 解构模式：
  - `const { data, error } = await client.GET(...)`
  - `if (error) throw error`
- 异步业务逻辑放在 store 层，组件仅消费 store 暴露的方法，不直接调用 API client。
- Zustand store 通过 `store/createStore.ts` 的工厂能力创建。
- TypeScript 类型导入使用 `import type`。

## 后端约束（apps/server）

- Controller 同时承担路由定义与处理器绑定，不新增独立 routes 层。
- HTTP 响应统一为 `{ code, msg, data }` envelope：成功 `code=0,msg='ok'`，错误 `code!=0,data=null`。
- 统一响应封装通过 `elysia-plugin-response` 的 `response()` 处理；controller/service 返回领域数据与异常语义，不手工重复包裹。
- model 声明层默认使用 `elysia-plugin-response` 内置的 `responseContract`（或同配置 contract），避免每个 model 重复初始化。
- Elysia model key 使用 `domain.purpose` 命名（如 `user.create`、`user.item`）。
- Service 使用静态类方法，不进行 service 实例化。
- 数据表与推导类型统一定义在 `src/db/schema.ts`，禁止手写重复的实体类型。
- 本地 TypeScript 导入使用 `.js` 扩展名，遵循 NodeNext 解析规则。
- 保持 `@/* -> src/*` 路径别名习惯，避免相对路径层级失控。

## 禁止项

- 不要手工编辑 `apps/web/src/services/schema.gen.ts`。
- 不要绕过 lint/test/hook 直接提交不可验证变更。
- 不要在 server 端重复定义可由 Drizzle `$inferSelect` 或 `$inferInsert` 推导的类型。
- 不要在组件中直接堆叠网络请求与业务状态编排逻辑。

## 变更执行建议

- 先做最小可行改动，再补充必要测试与文档。
- 涉及前后端联动时按顺序执行：后端改动 -> codegen -> 前端适配 -> lint/test。
- 大改动优先拆分为可审查的小提交，降低回归风险。
